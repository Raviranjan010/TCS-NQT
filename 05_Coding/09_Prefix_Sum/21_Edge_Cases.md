# Prefix Sum - Edge Cases

Check your code against these edge cases:
1.  **Empty Array / Input:** Handle $N = 0$ safely without throwing division by zero or index out of bound.
2.  **Single Element:** Verify behavior for $N = 1$.
3.  **All Duplicates:** Check if hash map frequencies handle identical keys.
